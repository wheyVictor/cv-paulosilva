<!-- session: unknown | trigger: auto -->
## Session Memory — 2026-03-09 10:05

**What happened**: Deployed new Santifer iRepair bridge page (`/santifer-irepair` ES, `/santifer-irepair-founder` EN) with hero, metrics cards, and CTAs. Then discovered sitemap was missing most project URLs and implemented automatic sitemap generation from registry during build.

**Key decisions**: 
- Decided to generate sitemap dynamically from `registry.ts` during build instead of maintaining static XML, because it prevents desync and ensures all new pages are automatically included.
- Decided to run sitemap generation after `vite build` but before `validate-articles`, because validation can then check sitemap completeness.
- Decided to delete `public/sitemap.xml` and generate to `dist/sitemap.xml`, because build output is the source of truth.

**Artifacts**:
- Created: `/Users/santifer/code/project2/cv-santiago/scripts/generate-sitemap.ts` (generates sitemap from registry with all ES/EN URLs, lastmod dates)
- Modified: `/Users/santifer/code/project2/cv-santiago/package.json` (added `generate-sitemap` script to build pipeline)
- Deleted: `/Users/santifer/code/project2/cv-santiago/public/sitemap.xml` (now generated during build)
- Created/Modified during deploy: `src/SantiferIRepair.tsx`, `src/irepair-i18n.ts`, `public/irepair/` assets, `registry.ts`, `App.tsx`, `vercel.json`

**Open threads**: 
- Script assumes registry entries have `slug`, `slugEn`, and `dateModified`—verify all registry entries have these fields or script will fail silently.
- Could add `validate-articles` check to ensure generated sitemap contains all registry entries and flags missing URLs as errors.

**Context for next session**: Sitemap generation is now automated; next step is to verify the generated sitemap in `dist/` includes all ~8 project pages (4 ES + 4 EN) after running `npm run build`. May need to add validation check to catch missing sitemap entries in future deploys.
