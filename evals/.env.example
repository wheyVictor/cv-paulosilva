# Copia este archivo a .env.local y añade tu API key
# .env.local está en .gitignore y no se sube a GitHub

# Requerido para el test "tone-quality" que usa LLM Judge (Claude Haiku)
ANTHROPIC_API_KEY=sk-ant-xxx
